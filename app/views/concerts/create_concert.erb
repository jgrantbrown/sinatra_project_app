<h1> Add a concert you have been too! </h1>

<form method="POST" action="/concert">

  <label> Bands: </label>
      <% Band.all.each do |band| %>
        <p>
          <label><%= band.name %></label>
          <input type="checkbox" name="bands[]" value="<%= band.id %>">
        </p>
      <% end %>

    <p>Add or create band</p>
    <input type='text' name=band[name]>
  </br>

  <label>Concert Name</label>
    <input type"text" name=concert[name] >
  <label>Concert Date</label>
    <input type"text" name=concert[date] ><br><br>

  <label>Venue Name</label>
  <% Venue.all.each do |venue| %>
    <p>
      <label><%= venue.name %></label>
      <input type="checkbox" name="venues[]" value="<%= venue.id %>">
    </p>
  <% end %>

    <p>Add or create band</p>
    <input type='text' name=venue[name]>

  <input type="submit" value="Add a Concert">
</form>
